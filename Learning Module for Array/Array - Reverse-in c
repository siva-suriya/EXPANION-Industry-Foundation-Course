Array - Reverse

problem://////////////////////

Input: 
Input String: "Mike"
Output: "ekiM"

Input String: "break"
Output: "kaerb"

code:///////////////////////////

#include "common.h"
#include <string.h>

char * func(char * str){
	// WRITE YOUR CODE HEREor()
	int i,k,b=0;
	if(str==NULL)
	{
	return NULL;
}
else{
    k=strlen(str);
    char *a;
    a=(char *)malloc(k*sizeof(char));
    for(i=k-1;i>=0;i--)
    {
        *(a+b)=*(str+i);
        b++;
    }
   return a; 
}
}
